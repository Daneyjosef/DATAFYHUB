# Get an API key at https://resend.com (free tier available)
RESEND_API_KEY=

# Sender address. Must be on a domain verified in Resend, otherwise
# fall back to "onboarding@resend.dev" (the sandbox sender).
BOOKING_FROM_EMAIL="Datafy Hub <bookings@datafy.ng>"
